"use strict";
function logDetails(uid, item) {
    console.log(`A product with ${uid} has a title as ${item}`);
}
function logInfo(uid, item) {
    console.log(`A product with ${uid} has a title as ${item}`);
}
logDetails(3, "olá");
logInfo("123", "Willian");
function Plats(Platform) {
    return Platform;
}
Plats("Linux");
